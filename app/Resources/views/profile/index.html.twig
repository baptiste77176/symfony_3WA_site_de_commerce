{% extends 'profile/base.profile.html.twig' %}

{% block colRight %}
    <div class="row my-4">
        <div class="col">
            <h1 class="mb-4">page de profil</h1>
            <p>Bonjour {{ app.user.username }}</p>
            {% include 'inc/flashMessage.html.twig' %}
            {#

                is_granted: tester le r�le de l'utilisateur ou son �tat de connexion
                    IS_AUTHENTICATED_FULLY : connect�
            #}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{  url('security.logout') }}">Déconnexion</a>
            {% endif %}
        </div>
    </div>
{% endblock %}
